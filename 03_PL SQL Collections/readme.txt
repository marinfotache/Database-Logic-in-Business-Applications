Only Oracle PL/SQL collections are covered (collections used in PL/SQL blocks).
Section related to the persistent (stored as table attributes) collections (including SQL queries for persistent collection) will be added in section 08.
